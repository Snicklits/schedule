# Backend URL for production (Railway, Render, etc.)
# Set this in Vercel project settings → Environment Variables
# Leave unset in local dev — the Vite proxy handles /api → localhost:3000
VITE_API_URL=https://your-backend.railway.app
